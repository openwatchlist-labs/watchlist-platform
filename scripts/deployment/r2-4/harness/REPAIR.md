# Accepted repair lineage

The reference harness incorporates four repair classes discovered during R2.4:

1. canonical path identity for symlink and process ownership checks;
2. exact commit-pinned catalog fixture bytes and package diagnostics;
3. passage-level corpus integrity plus exact manifest recompilation;
4. complete temporary-root configuration rebinding with declared seal
   regeneration and qualification-first archive ordering.

See `docs/homelab/r2-4/defect-closure.md` for the sanitized engineering record.
