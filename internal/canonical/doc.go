// Package canonical defines the shared enumerated types used across the
// matching, request, and provider packages: MessageDefinition,
// SemanticRole, PartyRole, ValueType, MatchRoute, CandidateType, and
// related constants. It has no dependencies on other internal packages
// and exists specifically so those types have one authoritative
// definition instead of being redeclared per package.
package canonical
