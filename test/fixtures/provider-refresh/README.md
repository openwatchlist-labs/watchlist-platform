# Phase 7C-D provider refresh fixtures

These fixtures model a provider-neutral catalog refresh. Provider component references are opaque metadata supplied by the provider; they are not platform list identities.

The ready refresh explicitly declares a provider component rename, one unmapped removal, and one addition. The active Phase 7C-C alert mapping remains attached to the stable Phase 7C-B component ID. The blocked refresh removes that mapped component without an approved rename.
