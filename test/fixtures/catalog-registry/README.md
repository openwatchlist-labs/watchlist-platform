# Phase 7C-B catalog-registry fixtures

All definitions in this directory are synthetic control-plane metadata. They contain no live watchlist records. The provider fixture intentionally uses a generic provider and provider-defined component reference so core registry behavior is not coupled to OpenSanctions or any commercial provider naming convention.
