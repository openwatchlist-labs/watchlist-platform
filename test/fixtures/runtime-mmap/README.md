# Phase 8A compiler-input fixture

`ofac-fixture.owcin` is deterministic `runtime-catalog-input/v1alpha1` exported from the committed synthetic OFAC Advanced XML catalog. Values are UTF-8 hex encoded so the line protocol is unambiguous and does not normalize or discard source text.

No live OFAC or provider bytes are committed here.
