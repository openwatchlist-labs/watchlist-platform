# Phase 7C-C alert-list mapping fixtures

These fixtures are synthetic organization configuration. They contain no provider dataset identifiers and no third-party list data.

Resolution is exact and case-sensitive on `(source_system_id, raw_list_name)`. The raw list name is preserved unchanged. Mapping versions target only stable Phase 7C-B catalog component IDs.
