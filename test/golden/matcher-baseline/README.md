# Phase 3A deterministic matcher goldens

These artifacts use synthetic OFAC and ISO 20022 fixtures. They cover near-match organization and individual names, alias/transliteration punctuation folding, exact-route delegation, entity-type mismatch suppression, and deterministic batch/replay output.
