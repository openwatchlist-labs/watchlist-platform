# Phase 7C-B registry golden

`registry.json` is the deterministic result of registering one official component, one provider-neutral component, three catalog versions, two forward activations, one rollback, and one provider activation. `verify.json` is the expected integrity summary. No full watchlist catalog rows are stored in this golden.
