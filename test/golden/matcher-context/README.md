# Phase 3B contextual matcher goldens

These files are deterministic outputs for the synthetic `pacs008-contextual.xml` fixture and the synthetic jurisdiction policy set.

They demonstrate:

- exact governed jurisdiction matching for `CU`;
- boundary-safe narrative matching for `CUBA` without matching `SCUBA`;
- primary-name and vessel-alias phrase windows;
- denial-context suppression for `Jordan Example`;
- Phase 3A fuzzy-name delegation;
- exact identifier and address delegation; and
- request/replay parity.

All list and policy records are synthetic test data and are not actual sanctions designations or production policy advice.
