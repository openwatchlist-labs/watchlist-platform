# Phase 9D deterministic goldens

These compact JSON files are byte-stable outputs from `cmd/iso20022-family` using `family-matrix-r1.json` and synthetic XML fixtures. They contain no real customer, payment, or sanctions data.

Each family has an evidence envelope and screening projection. `all-families.batch.json` proves ordered batch preservation across the complete supported matrix.
