# Phase 8A memory-mapped package goldens

`ofac-fixture.owmmap` is the Go reference encoding of the Phase 8A fixture. The Rust compiler must produce byte-identical output. `ofac-fixture.info.json` records lineage, section counts, and checksums.

The golden package is synthetic and contains no live watchlist data.
